name: RDP Bridge

on:
  repository_dispatch:
    types: [RDP]
  workflow_dispatch:
    inputs:
      ip:
        description: 'Target Tailscale IP'
        required: true
      username:
        description: 'Target Windows Username'
        required: false
      password:
        description: 'Target Windows Password'
        required: false

jobs:
  RDP:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Add Tailscale Repository & Install Dependencies
        run: |
          curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/noble.noarmor.gpg | sudo tee /usr/share/keyrings/tailscale-archive-keyring.gpg >/dev/null
          curl -fsSL https://pkgs.tailscale.com/stable/ubuntu/noble.tailscale-keyring.list | sudo tee /etc/apt/sources.list.d/tailscale.list

          sudo apt-get update
          sudo apt-get install -y tailscale xvfb freerdp2-x11 pulseaudio alsa-utils libasound2-plugins xdotool

      - name: Authenticate Tailscale
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=linux-rdp-bridge

      - name: Resolve IP & Credentials
        run: |
          TARGET_IP="${{ github.event.client_payload.ip || inputs.ip }}"
          TARGET_USER="${{ github.event.client_payload.username || inputs.username || secrets.RDP_USER || 'Cloud PC' }}"
          TARGET_PASS="${{ github.event.client_payload.password || inputs.password || secrets.RDP_PASSWORD || '123456' }}"

          echo "TARGET_IP=$TARGET_IP" >> $GITHUB_ENV
          echo "TARGET_USER=$TARGET_USER" >> $GITHUB_ENV
          echo "TARGET_PASS=$TARGET_PASS" >> $GITHUB_ENV

          echo "::add-mask::$TARGET_PASS"
          echo "::add-mask::$TARGET_USER"

      - name: Run Single RDP Session inside XVFB
        run: |
          echo "Connecting headless RDP session to $TARGET_IP..."

          # Initialize Virtual Display Buffer
          Xvfb :99 -screen 0 1920x1080x24 &
          XVFB_PID=$!
          export DISPLAY=:99
          sleep 2

          # Initialize Audio Subsystem
          pulseaudio --start --exit-idle-time=-1
          sleep 1

          # Background Keep-Alive Task
          (
            while true; do
              sleep 10
              xdotool key F15 2>/dev/null || true
            done
          ) &
          KEEP_ALIVE_PID=$!

          echo "[$(date)] Starting RDP Session to $TARGET_IP..."

          # Runs xfreerdp directly. Once disconnected, the script continues immediately.
          xfreerdp /v:"$TARGET_IP" \
                   /u:"$TARGET_USER" \
                   /p:"$TARGET_PASS" \
                   /size:1920x1080 \
                   /bpp:32 \
                   /gdi:hw \
                   /rfx \
                   /admin \
                   /cert-ignore \
                   /cert:ignore \
                   +clipboard \
                   /network:LAN \
                   /sound:sys:pulse || true

          echo "[$(date)] RDP Session disconnected. Cleaning up processes and finishing job..."

          # Clean up background tasks
          kill $KEEP_ALIVE_PID 2>/dev/null || true
          kill $XVFB_PID 2>/dev/null || true

          exit 0
